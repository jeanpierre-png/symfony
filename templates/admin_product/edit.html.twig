{% extends 'base.html.twig' %}

{% block body %}

<h1>Modifier {{ product.name }}</h1>

{{ form_start(form) }}

    {{ form_widget(form) }}

    <button type="submit">Enregistrer</button>

{{ form_end(form) }}

{% endblock %}